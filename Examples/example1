// Max is a method which takes two integers a and b as input and returns their maximum
method Max (a: int, b:int) returns (c: int)
// Post-condition which ensures that the method after executing always returns a value that is maximum of the two inputs
  ensures c >= a
  ensures c >= b
{
  if (a > b) 
     { return a; } 
  else 
     { return b; }
}
